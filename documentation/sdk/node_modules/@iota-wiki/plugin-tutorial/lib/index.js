"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateOptions = void 0;
async function pluginTutorial(context, options) {
    return {
        name: "@iota-wiki/plugin-tutorial",
        async contentLoaded({ actions }) {
            actions.setGlobalData(options);
        },
    };
}
exports.default = pluginTutorial;
function validateOptions({ options: userOptions, }) {
    const id = userOptions.title.normalize().toLowerCase().replace(/\W/, "-");
    const defaultOptions = {
        id,
        route: id,
    };
    return Object.assign(defaultOptions, userOptions);
}
exports.validateOptions = validateOptions;
