import type { OptionValidationContext, LoadContext, Plugin } from "@docusaurus/types";
export interface Options {
    title: string;
    description: string;
    tags: string[];
    source?: string;
    preview?: string;
}
export interface DefaultOptions {
    id: string;
    route: string;
}
export declare type UserOptions = Options & Partial<DefaultOptions>;
export declare type NormalizedOptions = Options & DefaultOptions;
export default function pluginTutorial(context: LoadContext, options: NormalizedOptions): Promise<Plugin>;
export declare function validateOptions({ options: userOptions, }: OptionValidationContext<UserOptions, NormalizedOptions>): NormalizedOptions;
